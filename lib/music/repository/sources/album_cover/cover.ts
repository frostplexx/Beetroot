
export function getCoverTag(filePath: string): string | null {
    // 1. Check if cover is added to file as tag
    // if it is strip that and download the cover from provider and save it into the album folder and update the
    // database with the new path and remove the tag from the file
    return null
}
